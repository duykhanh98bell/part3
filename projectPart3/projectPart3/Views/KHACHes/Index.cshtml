@model IEnumerable<projectPart3.Models.KHACH>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
@if (Session["tenKH"] != null)
{
    <table class="table">
        <tr>
            <th>
                Tên
            </th>
            <th>
                Tên đệm
            </th>
            <th>
                Email
            </th>
            <th>
                Tên tài khoản
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>

                <td>
                    @Html.ActionLink("Delete", "Delete", new { id = item.id_Khach })
                </td>
            </tr>
        }

    </table>
}
else
{
    <li>@Html.ActionLink("Đăng ký", "Register", "Login")</li>
    <li>@Html.ActionLink("Đăng nhập", "Login", "Login")</li>
}
